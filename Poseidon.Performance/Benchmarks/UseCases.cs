/*
*************************************************************
* This code is generated by custom generator  
*************************************************************/
namespace Poseidon.Performance.Benchmarks;
using Swagger.Poseidon;
using System;
using System.Collections.Concurrent;
using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;
using static Swagger.Poseidon.PoseidonAuth;


public partial class Benchmarks
{ 
    public static Task ParallelForEachAsync<T>(
            IEnumerable<T> source,
            int degree,
            Func<T, Task> body)
    {
        async Task AwaitPartition(IEnumerator<T> partition)
        {
            using (partition)
            {
                while (partition.MoveNext())
                {
                    await body(partition.Current);
                }
            }
        }

        return Task.WhenAll(
            Partitioner
                .Create(source)
                .GetPartitions(degree)
                .AsParallel()
                .Select(AwaitPartition));
    }
    
    public async Task<List<object>> GetReqsWithAuth(int batches)
    {
        var list = new List<object>();

        var CategorySequence = Enumerable
                                   .Range(0, TaskCount)
                                   .Select(_ => new Func<Task<object>>(() 
                                          => GetCategoryAsync()))
                                   .ToArray();
        var PaymentProviderSequence = Enumerable
                                   .Range(0, TaskCount)
                                   .Select(_ => new Func<Task<object>>(() 
                                          => GetPaymentProviderAsync()))
                                   .ToArray();
        var AddressSequence = Enumerable
                                   .Range(0, TaskCount)
                                   .Select(_ => new Func<Task<object>>(() 
                                          => GetAddressAsync()))
                                   .ToArray();
        var UserSequence = Enumerable
                                   .Range(0, TaskCount)
                                   .Select(_ => new Func<Task<object>>(() 
                                          => GetUserAsync()))
                                   .ToArray();
        var CreditCardSequence = Enumerable
                                   .Range(0, TaskCount)
                                   .Select(_ => new Func<Task<object>>(() 
                                          => GetCreditCardAsync()))
                                   .ToArray();
        var CartSequence = Enumerable
                                   .Range(0, TaskCount)
                                   .Select(_ => new Func<Task<object>>(() 
                                          => GetCartAsync()))
                                   .ToArray();
        var WishListSequence = Enumerable
                                   .Range(0, TaskCount)
                                   .Select(_ => new Func<Task<object>>(() 
                                          => GetWishListAsync()))
                                   .ToArray();
        var TaxRateSequence = Enumerable
                                   .Range(0, TaskCount)
                                   .Select(_ => new Func<Task<object>>(() 
                                          => GetTaxRateAsync()))
                                   .ToArray();
        var ProductSequence = Enumerable
                                   .Range(0, TaskCount)
                                   .Select(_ => new Func<Task<object>>(() 
                                          => GetProductAsync()))
                                   .ToArray();
        var DiscountOfferSequence = Enumerable
                                   .Range(0, TaskCount)
                                   .Select(_ => new Func<Task<object>>(() 
                                          => GetDiscountOfferAsync()))
                                   .ToArray();
        var ProductDiscountOfferSequence = Enumerable
                                   .Range(0, TaskCount)
                                   .Select(_ => new Func<Task<object>>(() 
                                          => GetProductDiscountOfferAsync()))
                                   .ToArray();
        var WishListItemSequence = Enumerable
                                   .Range(0, TaskCount)
                                   .Select(_ => new Func<Task<object>>(() 
                                          => GetWishListItemAsync()))
                                   .ToArray();
        var CartItemSequence = Enumerable
                                   .Range(0, TaskCount)
                                   .Select(_ => new Func<Task<object>>(() 
                                          => GetCartItemAsync()))
                                   .ToArray();
        var DeliverySequence = Enumerable
                                   .Range(0, TaskCount)
                                   .Select(_ => new Func<Task<object>>(() 
                                          => GetDeliveryAsync()))
                                   .ToArray();
        var OrderSequence = Enumerable
                                   .Range(0, TaskCount)
                                   .Select(_ => new Func<Task<object>>(() 
                                          => GetOrderAsync()))
                                   .ToArray();
        var PaymentSequence = Enumerable
                                   .Range(0, TaskCount)
                                   .Select(_ => new Func<Task<object>>(() 
                                          => GetPaymentAsync()))
                                   .ToArray();
        var OrderItemSequence = Enumerable
                                   .Range(0, TaskCount)
                                   .Select(_ => new Func<Task<object>>(() 
                                          => GetOrderItemAsync()))
                                   .ToArray();
        var UserProductReviewSequence = Enumerable
                                   .Range(0, TaskCount)
                                   .Select(_ => new Func<Task<object>>(() 
                                          => GetUserProductReviewAsync()))
                                   .ToArray();
        var SettingsSequence = Enumerable
                                   .Range(0, TaskCount)
                                   .Select(_ => new Func<Task<object>>(() 
                                          => GetSettingsAsync()))
                                   .ToArray();
 
        await ParallelForEachAsync(CategorySequence, batches, async func =>
        {
            list.Add(await func());
        });
        await ParallelForEachAsync(PaymentProviderSequence, batches, async func =>
        {
            list.Add(await func());
        });
        await ParallelForEachAsync(AddressSequence, batches, async func =>
        {
            list.Add(await func());
        });
        await ParallelForEachAsync(UserSequence, batches, async func =>
        {
            list.Add(await func());
        });
        await ParallelForEachAsync(CreditCardSequence, batches, async func =>
        {
            list.Add(await func());
        });
        await ParallelForEachAsync(CartSequence, batches, async func =>
        {
            list.Add(await func());
        });
        await ParallelForEachAsync(WishListSequence, batches, async func =>
        {
            list.Add(await func());
        });
        await ParallelForEachAsync(TaxRateSequence, batches, async func =>
        {
            list.Add(await func());
        });
        await ParallelForEachAsync(ProductSequence, batches, async func =>
        {
            list.Add(await func());
        });
        await ParallelForEachAsync(DiscountOfferSequence, batches, async func =>
        {
            list.Add(await func());
        });
        await ParallelForEachAsync(ProductDiscountOfferSequence, batches, async func =>
        {
            list.Add(await func());
        });
        await ParallelForEachAsync(WishListItemSequence, batches, async func =>
        {
            list.Add(await func());
        });
        await ParallelForEachAsync(CartItemSequence, batches, async func =>
        {
            list.Add(await func());
        });
        await ParallelForEachAsync(DeliverySequence, batches, async func =>
        {
            list.Add(await func());
        });
        await ParallelForEachAsync(OrderSequence, batches, async func =>
        {
            list.Add(await func());
        });
        await ParallelForEachAsync(PaymentSequence, batches, async func =>
        {
            list.Add(await func());
        });
        await ParallelForEachAsync(OrderItemSequence, batches, async func =>
        {
            list.Add(await func());
        });
        await ParallelForEachAsync(UserProductReviewSequence, batches, async func =>
        {
            list.Add(await func());
        });
        await ParallelForEachAsync(SettingsSequence, batches, async func =>
        {
            list.Add(await func());
        });
        return list;
    }

    public async Task<object> GetCategoryAsync()
    {  
        var res = await GetPoseidonAccessToken();
                HttpClient httpClient   = new();
                PoseidonClient poseidon = new (httpClient)
                {
                    Token = res.response?.Token!
                };
                
        var (page, size) = GetPageAndSize();
        
        var result = await poseidon.MediatrCategoryQueryAsync(page, size, null, null);

        return result!.Items!.Count;
    }
 
    public async Task<object> GetPaymentProviderAsync()
    {  
        var res = await GetPoseidonAccessToken();
                HttpClient httpClient   = new();
                PoseidonClient poseidon = new (httpClient)
                {
                    Token = res.response?.Token!
                };
                
        var (page, size) = GetPageAndSize();
        
        var result = await poseidon.MediatrPaymentproviderQueryAsync(page, size, null, null);

        return result!.Items!.Count;
    }
 
    public async Task<object> GetAddressAsync()
    {  
        var res = await GetPoseidonAccessToken();
                HttpClient httpClient   = new();
                PoseidonClient poseidon = new (httpClient)
                {
                    Token = res.response?.Token!
                };
                
        var (page, size) = GetPageAndSize();
        
        var result = await poseidon.MediatrAddressQueryAsync(page, size, null, null);

        return result!.Items!.Count;
    }
 
    public async Task<object> GetUserAsync()
    {  
        var res = await GetPoseidonAccessToken();
                HttpClient httpClient   = new();
                PoseidonClient poseidon = new (httpClient)
                {
                    Token = res.response?.Token!
                };
                
        var (page, size) = GetPageAndSize();
        
        var result = await poseidon.MediatrUserQueryAsync(page, size, null, null);

        return result!.Items!.Count;
    }
 
    public async Task<object> GetCreditCardAsync()
    {  
        var res = await GetPoseidonAccessToken();
                HttpClient httpClient   = new();
                PoseidonClient poseidon = new (httpClient)
                {
                    Token = res.response?.Token!
                };
                
        var (page, size) = GetPageAndSize();
        
        var result = await poseidon.MediatrCreditcardQueryAsync(page, size, null, null);

        return result!.Items!.Count;
    }
 
    public async Task<object> GetCartAsync()
    {  
        var res = await GetPoseidonAccessToken();
                HttpClient httpClient   = new();
                PoseidonClient poseidon = new (httpClient)
                {
                    Token = res.response?.Token!
                };
                
        var (page, size) = GetPageAndSize();
        
        var result = await poseidon.MediatrCartQueryAsync(page, size, null, null);

        return result!.Items!.Count;
    }
 
    public async Task<object> GetWishListAsync()
    {  
        var res = await GetPoseidonAccessToken();
                HttpClient httpClient   = new();
                PoseidonClient poseidon = new (httpClient)
                {
                    Token = res.response?.Token!
                };
                
        var (page, size) = GetPageAndSize();
        
        var result = await poseidon.MediatrWishlistQueryAsync(page, size, null, null);

        return result!.Items!.Count;
    }
 
    public async Task<object> GetTaxRateAsync()
    {  
        var res = await GetPoseidonAccessToken();
                HttpClient httpClient   = new();
                PoseidonClient poseidon = new (httpClient)
                {
                    Token = res.response?.Token!
                };
                
        var (page, size) = GetPageAndSize();
        
        var result = await poseidon.MediatrTaxrateQueryAsync(page, size, null, null);

        return result!.Items!.Count;
    }
 
    public async Task<object> GetProductAsync()
    {  
        var res = await GetPoseidonAccessToken();
                HttpClient httpClient   = new();
                PoseidonClient poseidon = new (httpClient)
                {
                    Token = res.response?.Token!
                };
                
        var (page, size) = GetPageAndSize();
        
        var result = await poseidon.MediatrProductQueryAsync(page, size, null, null);

        return result!.Items!.Count;
    }
 
    public async Task<object> GetDiscountOfferAsync()
    {  
        var res = await GetPoseidonAccessToken();
                HttpClient httpClient   = new();
                PoseidonClient poseidon = new (httpClient)
                {
                    Token = res.response?.Token!
                };
                
        var (page, size) = GetPageAndSize();
        
        var result = await poseidon.MediatrDiscountofferQueryAsync(page, size, null, null);

        return result!.Items!.Count;
    }
 
    public async Task<object> GetProductDiscountOfferAsync()
    {  
        var res = await GetPoseidonAccessToken();
                HttpClient httpClient   = new();
                PoseidonClient poseidon = new (httpClient)
                {
                    Token = res.response?.Token!
                };
                
        var (page, size) = GetPageAndSize();
        
        var result = await poseidon.MediatrProductdiscountofferQueryAsync(page, size, null, null);

        return result!.Items!.Count;
    }
 
    public async Task<object> GetWishListItemAsync()
    {  
        var res = await GetPoseidonAccessToken();
                HttpClient httpClient   = new();
                PoseidonClient poseidon = new (httpClient)
                {
                    Token = res.response?.Token!
                };
                
        var (page, size) = GetPageAndSize();
        
        var result = await poseidon.MediatrWishlistitemQueryAsync(page, size, null, null);

        return result!.Items!.Count;
    }
 
    public async Task<object> GetCartItemAsync()
    {  
        var res = await GetPoseidonAccessToken();
                HttpClient httpClient   = new();
                PoseidonClient poseidon = new (httpClient)
                {
                    Token = res.response?.Token!
                };
                
        var (page, size) = GetPageAndSize();
        
        var result = await poseidon.MediatrCartitemQueryAsync(page, size, null, null);

        return result!.Items!.Count;
    }
 
    public async Task<object> GetDeliveryAsync()
    {  
        var res = await GetPoseidonAccessToken();
                HttpClient httpClient   = new();
                PoseidonClient poseidon = new (httpClient)
                {
                    Token = res.response?.Token!
                };
                
        var (page, size) = GetPageAndSize();
        
        var result = await poseidon.MediatrDeliveryQueryAsync(page, size, null, null);

        return result!.Items!.Count;
    }
 
    public async Task<object> GetOrderAsync()
    {  
        var res = await GetPoseidonAccessToken();
                HttpClient httpClient   = new();
                PoseidonClient poseidon = new (httpClient)
                {
                    Token = res.response?.Token!
                };
                
        var (page, size) = GetPageAndSize();
        
        var result = await poseidon.MediatrOrderQueryAsync(page, size, null, null);

        return result!.Items!.Count;
    }
 
    public async Task<object> GetPaymentAsync()
    {  
        var res = await GetPoseidonAccessToken();
                HttpClient httpClient   = new();
                PoseidonClient poseidon = new (httpClient)
                {
                    Token = res.response?.Token!
                };
                
        var (page, size) = GetPageAndSize();
        
        var result = await poseidon.MediatrPaymentQueryAsync(page, size, null, null);

        return result!.Items!.Count;
    }
 
    public async Task<object> GetOrderItemAsync()
    {  
        var res = await GetPoseidonAccessToken();
                HttpClient httpClient   = new();
                PoseidonClient poseidon = new (httpClient)
                {
                    Token = res.response?.Token!
                };
                
        var (page, size) = GetPageAndSize();
        
        var result = await poseidon.MediatrOrderitemQueryAsync(page, size, null, null);

        return result!.Items!.Count;
    }
 
    public async Task<object> GetUserProductReviewAsync()
    {  
        var res = await GetPoseidonAccessToken();
                HttpClient httpClient   = new();
                PoseidonClient poseidon = new (httpClient)
                {
                    Token = res.response?.Token!
                };
                
        var (page, size) = GetPageAndSize();
        
        var result = await poseidon.MediatrUserproductreviewQueryAsync(page, size, null, null);

        return result!.Items!.Count;
    }
 
    public async Task<object> GetSettingsAsync()
    {  
        var res = await GetPoseidonAccessToken();
                HttpClient httpClient   = new();
                PoseidonClient poseidon = new (httpClient)
                {
                    Token = res.response?.Token!
                };
                
        var (page, size) = GetPageAndSize();
        
        var result = await poseidon.MediatrSettingsQueryAsync(page, size, null, null);

        return result!.Items!.Count;
    }
 

    public async Task<List<object>> PostReqsWithAuth(int batches,string? arg = null, bool update = false)
    {
        IgnoreId = !update;
        
        var list = new List<object>();

        var CategorySeq = Enumerable
                               .Range(0, TaskCount)
                               .Select(_ => new Func<Task<object>>(() 
                                      => PostCategoryAsync()))
                               .ToArray();
        var PaymentProviderSeq = Enumerable
                               .Range(0, TaskCount)
                               .Select(_ => new Func<Task<object>>(() 
                                      => PostPaymentProviderAsync()))
                               .ToArray();
        var AddressSeq = Enumerable
                               .Range(0, TaskCount)
                               .Select(_ => new Func<Task<object>>(() 
                                      => PostAddressAsync()))
                               .ToArray();
        var UserSeq = Enumerable
                               .Range(0, TaskCount)
                               .Select(_ => new Func<Task<object>>(() 
                                      => PostUserAsync()))
                               .ToArray();
        var CreditCardSeq = Enumerable
                               .Range(0, TaskCount)
                               .Select(_ => new Func<Task<object>>(() 
                                      => PostCreditCardAsync()))
                               .ToArray();
        var CartSeq = Enumerable
                               .Range(0, TaskCount)
                               .Select(_ => new Func<Task<object>>(() 
                                      => PostCartAsync()))
                               .ToArray();
        var WishListSeq = Enumerable
                               .Range(0, TaskCount)
                               .Select(_ => new Func<Task<object>>(() 
                                      => PostWishListAsync()))
                               .ToArray();
        var TaxRateSeq = Enumerable
                               .Range(0, TaskCount)
                               .Select(_ => new Func<Task<object>>(() 
                                      => PostTaxRateAsync()))
                               .ToArray();
        var ProductSeq = Enumerable
                               .Range(0, TaskCount)
                               .Select(_ => new Func<Task<object>>(() 
                                      => PostProductAsync()))
                               .ToArray();
        var DiscountOfferSeq = Enumerable
                               .Range(0, TaskCount)
                               .Select(_ => new Func<Task<object>>(() 
                                      => PostDiscountOfferAsync()))
                               .ToArray();
        var ProductDiscountOfferSeq = Enumerable
                               .Range(0, TaskCount)
                               .Select(_ => new Func<Task<object>>(() 
                                      => PostProductDiscountOfferAsync()))
                               .ToArray();
        var WishListItemSeq = Enumerable
                               .Range(0, TaskCount)
                               .Select(_ => new Func<Task<object>>(() 
                                      => PostWishListItemAsync()))
                               .ToArray();
        var CartItemSeq = Enumerable
                               .Range(0, TaskCount)
                               .Select(_ => new Func<Task<object>>(() 
                                      => PostCartItemAsync()))
                               .ToArray();
        var DeliverySeq = Enumerable
                               .Range(0, TaskCount)
                               .Select(_ => new Func<Task<object>>(() 
                                      => PostDeliveryAsync()))
                               .ToArray();
        var OrderSeq = Enumerable
                               .Range(0, TaskCount)
                               .Select(_ => new Func<Task<object>>(() 
                                      => PostOrderAsync()))
                               .ToArray();
        var PaymentSeq = Enumerable
                               .Range(0, TaskCount)
                               .Select(_ => new Func<Task<object>>(() 
                                      => PostPaymentAsync()))
                               .ToArray();
        var OrderItemSeq = Enumerable
                               .Range(0, TaskCount)
                               .Select(_ => new Func<Task<object>>(() 
                                      => PostOrderItemAsync()))
                               .ToArray();
        var UserProductReviewSeq = Enumerable
                               .Range(0, TaskCount)
                               .Select(_ => new Func<Task<object>>(() 
                                      => PostUserProductReviewAsync()))
                               .ToArray();
        var SettingsSeq = Enumerable
                               .Range(0, TaskCount)
                               .Select(_ => new Func<Task<object>>(() 
                                      => PostSettingsAsync()))
                               .ToArray();
 
        await ParallelForEachAsync(CategorySeq, batches, async func =>
        {
            list.Add(await func());
        });
        await ParallelForEachAsync(PaymentProviderSeq, batches, async func =>
        {
            list.Add(await func());
        });
        await ParallelForEachAsync(AddressSeq, batches, async func =>
        {
            list.Add(await func());
        });
        await ParallelForEachAsync(UserSeq, batches, async func =>
        {
            list.Add(await func());
        });
        await ParallelForEachAsync(CreditCardSeq, batches, async func =>
        {
            list.Add(await func());
        });
        await ParallelForEachAsync(CartSeq, batches, async func =>
        {
            list.Add(await func());
        });
        await ParallelForEachAsync(WishListSeq, batches, async func =>
        {
            list.Add(await func());
        });
        await ParallelForEachAsync(TaxRateSeq, batches, async func =>
        {
            list.Add(await func());
        });
        await ParallelForEachAsync(ProductSeq, batches, async func =>
        {
            list.Add(await func());
        });
        await ParallelForEachAsync(DiscountOfferSeq, batches, async func =>
        {
            list.Add(await func());
        });
        await ParallelForEachAsync(ProductDiscountOfferSeq, batches, async func =>
        {
            list.Add(await func());
        });
        await ParallelForEachAsync(WishListItemSeq, batches, async func =>
        {
            list.Add(await func());
        });
        await ParallelForEachAsync(CartItemSeq, batches, async func =>
        {
            list.Add(await func());
        });
        await ParallelForEachAsync(DeliverySeq, batches, async func =>
        {
            list.Add(await func());
        });
        await ParallelForEachAsync(OrderSeq, batches, async func =>
        {
            list.Add(await func());
        });
        await ParallelForEachAsync(PaymentSeq, batches, async func =>
        {
            list.Add(await func());
        });
        await ParallelForEachAsync(OrderItemSeq, batches, async func =>
        {
            list.Add(await func());
        });
        await ParallelForEachAsync(UserProductReviewSeq, batches, async func =>
        {
            list.Add(await func());
        });
        await ParallelForEachAsync(SettingsSeq, batches, async func =>
        {
            list.Add(await func());
        });
        return list;
    }

    public async Task<object> PostCategoryAsync()
    {  
        var res = await GetPoseidonAccessToken();

                HttpClient httpClient   = new();
                PoseidonClient poseidon = new (httpClient)
                {
                    Token = res.response?.Token!
                };
                
        CategoryCommand[] commands = DataGen<CategoryCommand>.Generate(skippIds: IgnoreId); 
        object result;
        try
        {
        
            result = await poseidon.MediatrCategoryUpsertAsync(commands[0]);
        } 
        catch(Exception ex)
        {
            result = 0; 
            Console.WriteLine($"Message: {ex.Message} \n Inner message: {ex.InnerException?.Message}");
        }
        return result;
    }
    public async Task<object> PostPaymentProviderAsync()
    {  
        var res = await GetPoseidonAccessToken();

                HttpClient httpClient   = new();
                PoseidonClient poseidon = new (httpClient)
                {
                    Token = res.response?.Token!
                };
                
        PaymentProviderCommand[] commands = DataGen<PaymentProviderCommand>.Generate(skippIds: IgnoreId); 
        object result;
        try
        {
        
            result = await poseidon.MediatrPaymentproviderUpsertAsync(commands[0]);
        } 
        catch(Exception ex)
        {
            result = 0; 
            Console.WriteLine($"Message: {ex.Message} \n Inner message: {ex.InnerException?.Message}");
        }
        return result;
    }
    public async Task<object> PostAddressAsync()
    {  
        var res = await GetPoseidonAccessToken();

                HttpClient httpClient   = new();
                PoseidonClient poseidon = new (httpClient)
                {
                    Token = res.response?.Token!
                };
                
        AddressCommand[] commands = DataGen<AddressCommand>.Generate(skippIds: IgnoreId); 
        object result;
        try
        {
        
            result = await poseidon.MediatrAddressUpsertAsync(commands[0]);
        } 
        catch(Exception ex)
        {
            result = 0; 
            Console.WriteLine($"Message: {ex.Message} \n Inner message: {ex.InnerException?.Message}");
        }
        return result;
    }
    public async Task<object> PostUserAsync()
    {  
        var res = await GetPoseidonAccessToken();

                HttpClient httpClient   = new();
                PoseidonClient poseidon = new (httpClient)
                {
                    Token = res.response?.Token!
                };
                
        UserCommand[] commands = DataGen<UserCommand>.Generate(skippIds: IgnoreId); 
        object result;
        try
        {
        
            result = await poseidon.MediatrUserUpsertAsync(commands[0]);
        } 
        catch(Exception ex)
        {
            result = 0; 
            Console.WriteLine($"Message: {ex.Message} \n Inner message: {ex.InnerException?.Message}");
        }
        return result;
    }
    public async Task<object> PostCreditCardAsync()
    {  
        var res = await GetPoseidonAccessToken();

                HttpClient httpClient   = new();
                PoseidonClient poseidon = new (httpClient)
                {
                    Token = res.response?.Token!
                };
                
        CreditCardCommand[] commands = DataGen<CreditCardCommand>.Generate(skippIds: IgnoreId); 
        object result;
        try
        {
        
            result = await poseidon.MediatrCreditcardUpsertAsync(commands[0]);
        } 
        catch(Exception ex)
        {
            result = 0; 
            Console.WriteLine($"Message: {ex.Message} \n Inner message: {ex.InnerException?.Message}");
        }
        return result;
    }
    public async Task<object> PostCartAsync()
    {  
        var res = await GetPoseidonAccessToken();

                HttpClient httpClient   = new();
                PoseidonClient poseidon = new (httpClient)
                {
                    Token = res.response?.Token!
                };
                
        CartCommand[] commands = DataGen<CartCommand>.Generate(skippIds: IgnoreId); 
        object result;
        try
        {
        
            result = await poseidon.MediatrCartUpsertAsync(commands[0]);
        } 
        catch(Exception ex)
        {
            result = 0; 
            Console.WriteLine($"Message: {ex.Message} \n Inner message: {ex.InnerException?.Message}");
        }
        return result;
    }
    public async Task<object> PostWishListAsync()
    {  
        var res = await GetPoseidonAccessToken();

                HttpClient httpClient   = new();
                PoseidonClient poseidon = new (httpClient)
                {
                    Token = res.response?.Token!
                };
                
        WishListCommand[] commands = DataGen<WishListCommand>.Generate(skippIds: IgnoreId); 
        object result;
        try
        {
        
            result = await poseidon.MediatrWishlistUpsertAsync(commands[0]);
        } 
        catch(Exception ex)
        {
            result = 0; 
            Console.WriteLine($"Message: {ex.Message} \n Inner message: {ex.InnerException?.Message}");
        }
        return result;
    }
    public async Task<object> PostTaxRateAsync()
    {  
        var res = await GetPoseidonAccessToken();

                HttpClient httpClient   = new();
                PoseidonClient poseidon = new (httpClient)
                {
                    Token = res.response?.Token!
                };
                
        TaxRateCommand[] commands = DataGen<TaxRateCommand>.Generate(skippIds: IgnoreId); 
        object result;
        try
        {
        
            result = await poseidon.MediatrTaxrateUpsertAsync(commands[0]);
        } 
        catch(Exception ex)
        {
            result = 0; 
            Console.WriteLine($"Message: {ex.Message} \n Inner message: {ex.InnerException?.Message}");
        }
        return result;
    }
    public async Task<object> PostProductAsync()
    {  
        var res = await GetPoseidonAccessToken();

                HttpClient httpClient   = new();
                PoseidonClient poseidon = new (httpClient)
                {
                    Token = res.response?.Token!
                };
                
        ProductCommand[] commands = DataGen<ProductCommand>.Generate(skippIds: IgnoreId); 
        object result;
        try
        {
        
            result = await poseidon.MediatrProductUpsertAsync(commands[0]);
        } 
        catch(Exception ex)
        {
            result = 0; 
            Console.WriteLine($"Message: {ex.Message} \n Inner message: {ex.InnerException?.Message}");
        }
        return result;
    }
    public async Task<object> PostDiscountOfferAsync()
    {  
        var res = await GetPoseidonAccessToken();

                HttpClient httpClient   = new();
                PoseidonClient poseidon = new (httpClient)
                {
                    Token = res.response?.Token!
                };
                
        DiscountOfferCommand[] commands = DataGen<DiscountOfferCommand>.Generate(skippIds: IgnoreId); 
        object result;
        try
        {
        
            result = await poseidon.MediatrDiscountofferUpsertAsync(commands[0]);
        } 
        catch(Exception ex)
        {
            result = 0; 
            Console.WriteLine($"Message: {ex.Message} \n Inner message: {ex.InnerException?.Message}");
        }
        return result;
    }
    public async Task<object> PostProductDiscountOfferAsync()
    {  
        var res = await GetPoseidonAccessToken();

                HttpClient httpClient   = new();
                PoseidonClient poseidon = new (httpClient)
                {
                    Token = res.response?.Token!
                };
                
        ProductDiscountOfferCommand[] commands = DataGen<ProductDiscountOfferCommand>.Generate(skippIds: IgnoreId); 
        object result;
        try
        {
        
            result = await poseidon.MediatrProductdiscountofferUpsertAsync(commands[0]);
        } 
        catch(Exception ex)
        {
            result = 0; 
            Console.WriteLine($"Message: {ex.Message} \n Inner message: {ex.InnerException?.Message}");
        }
        return result;
    }
    public async Task<object> PostWishListItemAsync()
    {  
        var res = await GetPoseidonAccessToken();

                HttpClient httpClient   = new();
                PoseidonClient poseidon = new (httpClient)
                {
                    Token = res.response?.Token!
                };
                
        WishListItemCommand[] commands = DataGen<WishListItemCommand>.Generate(skippIds: IgnoreId); 
        object result;
        try
        {
        
            result = await poseidon.MediatrWishlistitemUpsertAsync(commands[0]);
        } 
        catch(Exception ex)
        {
            result = 0; 
            Console.WriteLine($"Message: {ex.Message} \n Inner message: {ex.InnerException?.Message}");
        }
        return result;
    }
    public async Task<object> PostCartItemAsync()
    {  
        var res = await GetPoseidonAccessToken();

                HttpClient httpClient   = new();
                PoseidonClient poseidon = new (httpClient)
                {
                    Token = res.response?.Token!
                };
                
        CartItemCommand[] commands = DataGen<CartItemCommand>.Generate(skippIds: IgnoreId); 
        object result;
        try
        {
        
            result = await poseidon.MediatrCartitemUpsertAsync(commands[0]);
        } 
        catch(Exception ex)
        {
            result = 0; 
            Console.WriteLine($"Message: {ex.Message} \n Inner message: {ex.InnerException?.Message}");
        }
        return result;
    }
    public async Task<object> PostDeliveryAsync()
    {  
        var res = await GetPoseidonAccessToken();

                HttpClient httpClient   = new();
                PoseidonClient poseidon = new (httpClient)
                {
                    Token = res.response?.Token!
                };
                
        DeliveryCommand[] commands = DataGen<DeliveryCommand>.Generate(skippIds: IgnoreId); 
        object result;
        try
        {
        
            result = await poseidon.MediatrDeliveryUpsertAsync(commands[0]);
        } 
        catch(Exception ex)
        {
            result = 0; 
            Console.WriteLine($"Message: {ex.Message} \n Inner message: {ex.InnerException?.Message}");
        }
        return result;
    }
    public async Task<object> PostOrderAsync()
    {  
        var res = await GetPoseidonAccessToken();

                HttpClient httpClient   = new();
                PoseidonClient poseidon = new (httpClient)
                {
                    Token = res.response?.Token!
                };
                
        OrderCommand[] commands = DataGen<OrderCommand>.Generate(skippIds: IgnoreId); 
        object result;
        try
        {
        
            result = await poseidon.MediatrOrderUpsertAsync(commands[0]);
        } 
        catch(Exception ex)
        {
            result = 0; 
            Console.WriteLine($"Message: {ex.Message} \n Inner message: {ex.InnerException?.Message}");
        }
        return result;
    }
    public async Task<object> PostPaymentAsync()
    {  
        var res = await GetPoseidonAccessToken();

                HttpClient httpClient   = new();
                PoseidonClient poseidon = new (httpClient)
                {
                    Token = res.response?.Token!
                };
                
        PaymentCommand[] commands = DataGen<PaymentCommand>.Generate(skippIds: IgnoreId); 
        object result;
        try
        {
        
            result = await poseidon.MediatrPaymentUpsertAsync(commands[0]);
        } 
        catch(Exception ex)
        {
            result = 0; 
            Console.WriteLine($"Message: {ex.Message} \n Inner message: {ex.InnerException?.Message}");
        }
        return result;
    }
    public async Task<object> PostOrderItemAsync()
    {  
        var res = await GetPoseidonAccessToken();

                HttpClient httpClient   = new();
                PoseidonClient poseidon = new (httpClient)
                {
                    Token = res.response?.Token!
                };
                
        OrderItemCommand[] commands = DataGen<OrderItemCommand>.Generate(skippIds: IgnoreId); 
        object result;
        try
        {
        
            result = await poseidon.MediatrOrderitemUpsertAsync(commands[0]);
        } 
        catch(Exception ex)
        {
            result = 0; 
            Console.WriteLine($"Message: {ex.Message} \n Inner message: {ex.InnerException?.Message}");
        }
        return result;
    }
    public async Task<object> PostUserProductReviewAsync()
    {  
        var res = await GetPoseidonAccessToken();

                HttpClient httpClient   = new();
                PoseidonClient poseidon = new (httpClient)
                {
                    Token = res.response?.Token!
                };
                
        UserProductReviewCommand[] commands = DataGen<UserProductReviewCommand>.Generate(skippIds: IgnoreId); 
        object result;
        try
        {
        
            result = await poseidon.MediatrUserproductreviewUpsertAsync(commands[0]);
        } 
        catch(Exception ex)
        {
            result = 0; 
            Console.WriteLine($"Message: {ex.Message} \n Inner message: {ex.InnerException?.Message}");
        }
        return result;
    }
    public async Task<object> PostSettingsAsync()
    {  
        var res = await GetPoseidonAccessToken();

                HttpClient httpClient   = new();
                PoseidonClient poseidon = new (httpClient)
                {
                    Token = res.response?.Token!
                };
                
        SettingsCommand[] commands = DataGen<SettingsCommand>.Generate(skippIds: IgnoreId); 
        object result;
        try
        {
        
            result = await poseidon.MediatrSettingsUpsertAsync(commands[0]);
        } 
        catch(Exception ex)
        {
            result = 0; 
            Console.WriteLine($"Message: {ex.Message} \n Inner message: {ex.InnerException?.Message}");
        }
        return result;
    }
}


